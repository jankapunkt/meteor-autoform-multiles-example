import { Meteor } from 'meteor/meteor';

import {ProductImages} from '../imports/product_images'
import {Products} from '../imports/products'

Meteor.startup(() => {
  // code to run on server at startup
});
